<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd" []>
<task xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" id="inserting-required-element" xsi:noNamespaceSchemaLocation="urn:oasis:names:tc:dita:xsd:task.xsd:1.1">
  <title><?serna-redline-start 0 to be refactored?>Inserting Required Element<?serna-redline-end?></title>
  <taskbody>
    <context>
<?serna-redline-start 0 incorrect: choice appears when schema requires element among several choices, and only user can choose amongst them. ?>      <p>Green <codeph>Choose</codeph> elements  <image href="choose.png" align="center" placement="inline"/>  appear whenever the insertion of an element is <i>required</i>.</p>
<?serna-redline-end?>      <p><?serna-redline-start 0 it is noncence, Serna forces user to insert required element?>To insert a required element<?serna-redline-end?>, do the following:</p>
    </context>
    <steps>
      <step>
        <cmd>Call up the <keyword>Insert Element Dialog</keyword>.</cmd>
        <info>
          <p>To call up the <keyword>Insert Element Dialog</keyword> (or move the focus to the docked <keyword>Insert Element Dialog</keyword>), do one of the following:</p>
        </info>
        <choices>
          <choice>
            <p>Click the <codeph>Choose</codeph> element in the Editing Window.</p>
          </choice>
          <choice>
            <p>Right-click the  <codeph>Choose</codeph> element in the Editing Window and select <uicontrol>Insert Element</uicontrol> from the context menu.</p>
          </choice>
          <choice>
            <p>Right-click the <codeph>#choice</codeph> element in the ContentMap and select <uicontrol>Insert Element</uicontrol> from the context menu.</p>
          </choice>
          <choice>
            <p>If  the cursor is already on the <codeph>Choose</codeph> element, it is enough to press <uicontrol>Enter</uicontrol>.</p>
          </choice>
          <choice>
            <p>To move the cursor to the next <codeph>Choose</codeph> element and call up the <keyword>Insert Element Dialog</keyword>, press <uicontrol>Ctrl-Alt-Enter</uicontrol>  .</p>
          </choice>
        </choices>
        <stepresult conref="inserting-element.xml#inserting-element/dialog-appears"/>
      </step>
      <step conref="inserting-element.xml#inserting-element/select-element">
        <cmd/>
      </step>
    </steps>
    <example>
      <title>Example. Inserting required elements.</title>
      <ol>
        <li>Create a new document:<ol>
            <li>Choose <menucascade>
                <uicontrol>Document</uicontrol>
                <uicontrol>New Document</uicontrol>
              </menucascade> to call up the <keyword>New Document Dialog</keyword>.</li>
            <li>Select <codeph>Docbook V4.5</codeph>-&gt;<codeph>All</codeph>. Click <uicontrol>OK</uicontrol>.</li>
            <li>From the <uicontrol>Root Element</uicontrol> drop-down list, select <codeph>section</codeph>. Click <uicontrol>OK</uicontrol>.</li>
          </ol><image href="new-doc-section.png" align="center" placement="break"/></li>
        <li>
          <p>Click the <codeph>Choose</codeph> element.</p>
        </li>
        <li>
          <p>In the <keyword>Insert Element Dialog</keyword>, double-click the <codeph>title</codeph> element.</p>
        </li>
        <li>
          <p>The <codeph>title</codeph> element is inserted.</p>
          <fig frame="all">
            <image href="inserted-title.png" align="center" placement="break"/>
          </fig>
        </li>
        <li>
          <p>Let&apos;s type some text there. For example, &quot;<codeph>Citrus Fruit</codeph>&quot;.</p>
          <fig frame="all">
            <image href="typed-title-2.png" align="center" placement="break"/>
          </fig>
        </li>
        <li>
          <p>You see one more <codeph>Choose</codeph> element. Either click it or press <uicontrol>Ctrl-Alt-Enter</uicontrol>.</p>
        </li>
        <li>
          <p>In the <keyword>Insert Element Dialog</keyword>, double-click the <codeph>para</codeph> element.</p>
        </li>
        <li>
          <p>The <codeph>para</codeph> is inserted. The absence of <codeph>Choose</codeph> elements means you are not required to insert more elements.</p>
          <fig frame="all">
            <image href="inserted-para.png" align="center" placement="break"/>
          </fig>
        </li>
        <li>
          <p>However, let&apos;s insert more elements. Now to call up the <keyword>Insert Element Dialog</keyword>, press <uicontrol>Ctrl-Enter</uicontrol>.</p>
        </li>
        <li>
          <p>Dockable dialogs can be docked to different sides of the program window. Right-click the <keyword>Insert Element Dialog</keyword>  and select the <menucascade>
              <uicontrol>Dock Right</uicontrol>
            </menucascade> item from the menu.</p>
          <image href="dock-menu.png" align="center" placement="break"/>
        </li>
        <li>
          <p>The <keyword>Insert Element Dialog </keyword> is docked to the right side of the program window. Now whenever you press <uicontrol>Ctrl-Enter</uicontrol>,  the focus is moved to the dialog.</p>
          <image href="docked-dialog.png" align="center" placement="break"/>
        </li>
        <li>
          <p>The cursor is in the <codeph>para</codeph>. Press <uicontrol>Ctrl-Enter</uicontrol> to move the focus to the <keyword>Insert Element Dialog</keyword>.</p>
          <p>Insert the <codeph>itemizedlist</codeph> element.</p>
          <fig frame="all">
            <image href="cursor-choose.png" align="center" placement="break"/>
          </fig>
        </li>
        <li>
          <p>The cursor is on the <codeph>Choose</codeph> element. Press <uicontrol>Enter</uicontrol> to move the focus to the <keyword>Insert Element Dialog</keyword>. </p>
          <p>Insert the <codeph>listitem</codeph> element.</p>
          <fig frame="all">
            <image href="cursor-choose-2.png" align="center" placement="break"/>
          </fig>
        </li>
        <li>
          <p>Press <uicontrol>Enter</uicontrol> to move the focus to the <keyword>Insert Element Dialog</keyword>.</p>
          <p>Insert the <codeph>para</codeph> element.</p>
          <fig frame="all">
            <image href="inserted-listitem.png" align="center" placement="break"/>
          </fig>
        </li>
      </ol>
    </example>
  </taskbody>
</task>
